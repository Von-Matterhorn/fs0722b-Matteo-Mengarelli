# project

Per far funzionare il progetto assicurarsi di aver in locale un database attivo, possibilmente utilizzando PostgreSQL e pgAdmin, con una tabella contenente un catalogo.
Le seguenti proprietà devono essere assegnate ad ogni elemento:
id            int4
title         varchar
img           varchar
players       varchar
genre         varchar
price         varchar
platform      varchar
release       varchar
editor        varchar
voices        varchar
languages     varchar
info          varchar

Aprire la cartella app dentro un editor di codice per java, consiglio eclipse per estensione maven e springboot, ed eseguire il progetto dopo aver regolato le application.properties per far funzionare il progetto sul vostro computer

Aprire la cartella angular con un editor di codice, possibilmente Visual Studio Code, eseguire da terminale il comando npm i seguito dal comando ng s -o per aprireuna finestra del browser con il progetto

Per qualsiasi dubbio o report di bug, siete liberi di contattarmi 